
class Matricula {
    constructor(id_matricula, ativa, id_aluno, id_curso) {
        this.id_matricula = id_matricula,
            this.ativa = ativa,
            this.id_aluno = id_aluno,
            this.id_curso = id_curso
    }
}

export default Matricula;