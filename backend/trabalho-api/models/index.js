
class Trabalho {

    constructor(id, descricao, data_trabalho, id_professor) {
        this.id = id,
            this.descricao = descricao,
            this.data_trabalho = data_trabalho,
            this.id_professor = id_professor
    }

}

export default Trabalho;